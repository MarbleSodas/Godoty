[pytest]
markers =
    e2e: End-to-end tests requiring external services (Stripe, Supabase)
    integration: Integration tests requiring external services
    asyncio: Asyncio tests
asyncio_mode = strict
testpaths = tests
