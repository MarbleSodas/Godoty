Resource copying in  now returns  if source files are missing, ensuring the app doesn't start in a broken state.
Resource copying in setup.rs now returns Err if source files are missing, ensuring the app doesn't start in a broken state.
